# Base URL for the COLA API. In dev this is a same-origin path proxied by Vite
# to the FastAPI server (see vite.config.js). In production the SPA is served by
# the same host as the API, so the relative /api path also works.
VITE_API_BASE=/api
